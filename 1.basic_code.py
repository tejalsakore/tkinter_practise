import tkinter
window = tkinter.Tk()       #To create a main window, tkinter offers a method & window is the name of the main window object
window.title("GUI")         #to rename the title of the window
window.mainloop()           # mainloop() is used when your application is ready to run. mainloop() is an infinite loop used to run the application, wait for an event to occur and process the event as long as the window is not closed.